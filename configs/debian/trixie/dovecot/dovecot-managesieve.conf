# DOVECOT(1) ManageSieve configuration file - auto-generated by i-MSCP
#     DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN
#
# 2.4 removed the plugin {} block, so sieve settings are global, and the
# 'file:~/sieve;active=~/.dovecot.sieve' location string is replaced by a
# named sieve_script block with discrete settings. mail_plugins is a boolean
# list rather than a space separated string.

sieve_script personal {
    sieve_script_driver = file
    sieve_script_type = personal
    sieve_script_path = ~/sieve
    sieve_script_active_path = ~/.dovecot.sieve
}

protocol lda {
    mail_plugins {
        sieve = yes
    }
}
